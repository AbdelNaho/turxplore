# Sanity — no live project connected yet. Run `pnpm dlx sanity login` and
# create a project, then fill these in; the Studio at /studio and all
# content fetching pick them up automatically.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Set once the production domain is confirmed live.
NEXT_PUBLIC_SITE_URL=https://turxplore.com

# Phase D — inquiry form (Resend) and Plausible analytics.
RESEND_API_KEY=
INQUIRY_NOTIFICATION_EMAIL=hello@turxplore.com
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=turxplore.com
